# Backend Environment Variables Configuration
# 複製此檔案為 .env 並填入實際值

# ==========================================
# Server Configuration
# ==========================================

# 伺服器運行端口
PORT=3000

# Node 環境（development / production / test）
NODE_ENV=development

# ==========================================
# Database Configuration
# ==========================================

# SQLite 資料庫檔案路徑
# 開發環境使用 file:./prisma/dev.db
# 測試環境使用 file:./prisma/test.db
DATABASE_URL=file:./prisma/dev.db

# ==========================================
# Authentication & Security
# ==========================================

# JWT Secret Key（必須至少 32 字元的強隨機字串）
# 產生方式：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ⚠️ 請勿使用預設值，務必產生新的隨機字串！
JWT_SECRET=your_super_secret_jwt_key_at_least_32_characters_long_replace_this

# ==========================================
# Google Maps API Configuration
# ==========================================

# Google Maps Server API Key（後端使用）
# 需啟用以下 API：
#   - Geocoding API
#   - Places API  
#   - Directions API
#   - Distance Matrix API
#
# 安全性設定建議：
#   1. 應用程式限制：IP 位址（設定伺服器 IP）
#   2. API 限制：僅限上述 4 個 API
#   3. 定期輪換金鑰
#
# ⚠️ 重要：請勿將真實的 API Key 提交到 Git！
# ⚠️ 若無 IP 限制，請在 README.md 中說明風險
GOOGLE_MAPS_API_KEY=your_google_maps_server_api_key_here

# ==========================================
# Optional: Rate Limiting (已在程式碼中設定預設值)
# ==========================================

# Google Maps API 速率限制（每分鐘請求數）
# MAPS_RATE_LIMIT=120

# ==========================================
# Optional: Caching (已在程式碼中設定預設值)
# ==========================================

# 快取 TTL（秒）
# CACHE_TTL=600

# ==========================================
# 設定步驟
# ==========================================
# 
# 1. 複製此檔案：
#    cp .env.example .env
#
# 2. 產生 JWT Secret：
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#    將輸出結果填入 JWT_SECRET
#
# 3. 取得 Google Maps API Key：
#    - 前往 https://console.cloud.google.com/
#    - 建立或選擇專案
#    - 啟用必要的 API（見上方說明）
#    - 建立憑證（API 金鑰）
#    - 設定金鑰限制（建議 IP 限制）
#    - 將金鑰填入 GOOGLE_MAPS_API_KEY
#
# 4. 初始化資料庫：
#    npm run prisma:migrate
#
# 5. 啟動開發伺服器：
#    npm run dev
#
